var gnuplot = require('../gnuplot'),
    fs = require('fs');

var data = fs.createReadStream('plot.dat'),
    out = fs.createWriteStream('output.svg'),
    plotter = gnuplot().set('term svg');
    
data.pipe(plotter).pipe(out);